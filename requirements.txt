# NL2SQL (defog/sqlcoder-7b-2 + QLoRA) — v46 명세 기반 복원
# Python 3.10 기준

# --- 모델 / 파인튜닝 ---
torch>=2.1.0
transformers>=4.38.0
peft>=0.9.0
bitsandbytes>=0.42.0 # 4-bit NF4 양자화 (QLoRA)
accelerate>=0.27.0
datasets>=2.17.0

# --- RAG / 임베딩 ---
sentence-transformers>=2.5.0 # paraphrase-multilingual-MiniLM-L12-v2 (384차원)
scikit-learn>=1.3.0 # 코사인 유사도
numpy>=1.24.0

# --- 평가 (MySQL 실행 기반 TYPE_A 보정) ---
mysql-connector-python>=8.3.0
sqlparse>=0.4.4 # SQL 정규화/파싱

# --- 웹 챗봇 프론트엔드 ---
flask>=3.0.0 # app.py (질문→SQL→DB실행→시각화 API + 정적 UI)

# --- 유틸 ---
tqdm>=4.66.0
